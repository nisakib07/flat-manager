import type { NextConfig } from "next";
// Note: next-pwa is not currently compatible with Turbopack (Next.js 16 default bundler)
// PWA features will work through manifest.json and the install prompt component

const nextConfig: NextConfig = {
  /* config options here */
}

export default nextConfig

